<div class="row">
  <div class="small-6 large-centered columns">
    <%= simple_form_for([@project, @membership]) do |f| %>

      <%= f.input :role, as: :radio_buttons, collection: ['creator', 'collaborator'] %>
      <%= f.button :submit, 'Update Role' %>

    <% end %>
  </div>
</div>
